{
  "name": "remotyy-web",
  "version": "0.1.0",
  "private": true,
  "description": "remotyy web client — remote terminal in your browser",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0",
    "xterm-addon-web-links": "^0.9.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
